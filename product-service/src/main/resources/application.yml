server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_db
    username: root
    password: Admin@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # VERY IMPORTANT (existing tables)
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: product-service

